/**
	UI Constants

	These are Constants for UI elements. Rather than storing them in Constants, we will store them here
**/
class UI {
	public static final TooltipDelay: Float = 0.20;
	public static final HoverDelay: Float = 0.15;
}
